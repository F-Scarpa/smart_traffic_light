/*
#include <stdio.h>
#include "esp_log.h"

void app_main(void)
{   
    ESP_LOGE("LOG","This is an error");           //print a message as an error in the terminal (Red)
    ESP_LOGW("LOG","This is a warning");          //print a message as a warning in the terminal (Yellow)
    ESP_LOGI("LOG","This is an info");            //print a message as an info in the terminal (green)
    //ESP_LOGV("LOG","VERBOSE");
}
*/